import React from 'react';
import ThreadsPlaceholder from './ThreadsPlaceholder.jsx';

export default function ThreadsNotifications() {
  return (
    <ThreadsPlaceholder title="Notifications" hint="Mentions, suivis et réponses à vos threads." />
  );
}
