exports.generatePostIdeas = (platform) => {
  return [
    `Generate a creative ${platform} caption with emojis`,
    `Use trending audio for your next ${platform} post`,
    `Schedule a carousel-style ${platform} update for the weekend`
  ];
};
